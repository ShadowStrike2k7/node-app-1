<!DOCTYPE html>
<html lang="en">
<%- include('includes/head') %>
    <body>
        <%- include('includes/header') %>
        <main>
            <h1><%= post.title %></h1>

            <article>
                <small>
                    Posted by <%= post.username %> on <%= new Date(post.createdDate).getDate()%>/<%= new Date(post.createdDate).getMonth() + 1 %>/<%= new Date(post.createdDate).getFullYear() %>
                </small>
                <div>
                    <p><%- filterUserHtml(post.body) %> </p>
                </div>
            </article>
            <% if(isAuthor){ %>
            <a href="/edit-post/<%= post.id %>" class="button">Edit</a>
            <form action="/delete-post/<%= post.id %>" method="POST" style="display: inline;">
                <button>Delete</button>
            </form>
            <% } %>
        </main>
    
    </body>
</html>